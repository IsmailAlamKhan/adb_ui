name: Build Windows
on:
  workflow_call:
    inputs:
      version:
        description: "Version"
        required: true
        default: "0.0.0"
        type: string
      release-upload-url:
        description: "Release upload URL"
        required: true
        type: string
      app-name:
        description: "Application name"
        required: true
        type: string

jobs:
  build:
    runs-on: windows-latest
    env:
      RELEASE_PATH: build/windows/runner/Release
      # APPNAME: ${{ github.event.inputs.app-name }}
      # VERSION: ${{ github.event.inputs.version }}
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Install and set Flutter version
        uses: subosito/flutter-action@v2.4.0

      - name: Build Windows app
        run: flutter build windows --release

      - name: Run Installers/installer.iss Inno script
        run: |
          "%programfiles(x86)%\Inno Setup 6\iscc.exe" /dversion=${{github.event.inputs.version}} "Installers/installer.iss"
        shell: cmd

      - name: upload zip to release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ inputs.release-upload-url }}
          asset_path: Installers/${{github.event.inputs.app-name}}-setup-windows.exe
          asset_name: ${{github.event.inputs.app-name}}-setup-windows.exe
          asset_content_type: application/x-msdownload

      - name: Archive Release
        uses: thedoctor0/zip-release@master
        with:
          type: "zip"
          filename: ${{ github.event.inputs.app-name }}-windows.zip
          directory: ${{ env.RELEASE_PATH }}

      - name: upload zip to release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ inputs.release-upload-url }}
          asset_path: ${{env.RELEASE_PATH}}/${{github.event.inputs.app-name}}-windows.zip
          asset_name: ${{github.event.inputs.app-name}}-windows.zip
          asset_content_type: application/zip
